name: dark_router_example
description: Example project for dark_router package.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  json_annotation: ^4.9.0
  dark_router: ^0.0.1

dev_dependencies:
  json_serializable: ^6.8.0
  build_runner: ^2.4.0
